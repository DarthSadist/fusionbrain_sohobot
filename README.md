# FusionBrain SohoBot 🎨

Telegram-бот для генерации изображений с использованием API FusionBrain и дополнительными функциями обработки изображений.

## 🌟 Основные возможности

- 🎨 Генерация изображений по текстовому описанию
- 🖼 Удаление фона с изображений
- 🎭 Различные стили генерации
- 📏 Настраиваемые размеры изображений
- 💾 Кэширование результатов для оптимизации
- 📝 Подробное логирование операций

## 🛠 Технический стек

- Python 3.8+
- aiogram 3.x - для работы с Telegram API
- Pillow - для обработки изображений
- rembg - для удаления фона
- logging - для системы логирования
- aiohttp - для асинхронных HTTP-запросов

## 📋 Требования к системе

### Системные зависимости
```bash
sudo apt-get update
sudo apt-get install -y python3-pip python3-venv libmagic1
```

### Python зависимости
```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

## ⚙️ Настройка

1. Создайте файл `.env` в корневой директории проекта:
```env
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
FUSION_BRAIN_API_KEY=your_fusionbrain_api_key
FUSION_BRAIN_SECRET_KEY=your_fusionbrain_secret_key
```

2. Настройте параметры в файле `config.py`:
```python
MAX_PROMPT_LENGTH = 500  # Максимальная длина промпта
MAX_IMAGE_SIZE = 1500    # Максимальный размер изображения
```

## 🚀 Запуск

```bash
source venv/bin/activate
python main.py
```

## 📁 Структура проекта

```
fusionbrain_sohobot/
├── main.py                 # Основной файл бота
├── config.py              # Конфигурация
├── requirements.txt       # Зависимости Python
├── .env                  # Переменные окружения
├── logs/                 # Директория для логов
└── src/
    ├── constants/        # Константы и перечисления
    │   ├── messages.py   # Шаблоны сообщений
    │   └── styles.py     # Стили изображений
    ├── handlers/         # Обработчики команд
    │   ├── command_handlers.py
    │   └── callback_handlers.py
    └── utils/           # Вспомогательные модули
        ├── image_processor.py  # Обработка изображений
        └── logger.py     # Настройка логирования

```

## 🎨 Доступные стили изображений

- DEFAULT - Стандартный стиль
- ANIME - Аниме стиль
- CYBERPUNK - Киберпанк
- KANDINSKY - Стиль Кандинского
- POP_ART - Поп-арт
- И другие...

## 📏 Доступные размеры

- square (1024x1024)
- portrait (1024x1536)
- landscape (1536x1024)

## 🤖 Команды бота

- `/start` - Начало работы с ботом
- `/help` - Показать справку
- Кнопки интерфейса:
  - 🎨 Создать - Начать генерацию
  - 🖼 Удалить фон - Удалить фон с изображения
  - ⚙️ Настройки - Изменить размер изображения
  - 🎭 Стили - Выбрать стиль генерации
  - ↩️ Назад - Вернуться в главное меню

## 🔄 Процесс генерации

1. Пользователь вводит описание желаемого изображения
2. Бот проверяет и подготавливает промпт
3. Отправляет запрос к API FusionBrain
4. Отслеживает статус генерации
5. Получает и обрабатывает результат
6. Отправляет готовое изображение пользователю

## 🖼 Удаление фона

1. Изображение предварительно масштабируется (если необходимо)
2. Применяется алгоритм rembg
3. Результат кэшируется для повторного использования
4. При необходимости восстанавливается исходный размер

## 📝 Логирование

Система логирования включает:
- Информацию о действиях пользователя
- Статусы операций
- Ошибки и предупреждения
- Время выполнения операций
- Размеры и форматы изображений

Логи сохраняются в:
- Консоль (с цветовой дифференциацией)
- Файл `logs/bot.log` (с ротацией)

## 🔒 Безопасность

- Все чувствительные данные хранятся в `.env`
- Реализована защита от превышения лимитов API
- Проверка входных данных
- Безопасная обработка ошибок

## 🔧 Оптимизация

- Кэширование результатов удаления фона
- Асинхронная обработка запросов
- Оптимизация размера изображений
- Управление памятью при обработке

## 🐛 Отладка

Для включения подробного логирования:
```python
import logging
logging.basicConfig(level=logging.DEBUG)
```

## 👥 Разработка

### Добавление нового стиля

1. Добавьте стиль в `IMAGE_STYLES`:
```python
IMAGE_STYLES = {
    "NEW_STYLE": {
        "label": "Новый стиль",
        "prompt_prefix": "style description, ",
        "description": "Описание стиля",
        "example": "Пример использования"
    }
}
```

### Добавление новой функции обработки

1. Создайте метод в `ImageProcessor`
2. Добавьте обработчик в `main.py`
3. Обновите клавиатуру
4. Добавьте сообщения в `Messages`

## 📈 Масштабирование

Бот разработан с учетом возможности масштабирования:
- Модульная архитектура
- Отделение бизнес-логики
- Конфигурируемые параметры
- Система кэширования

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Отправьте pull request

## 📄 Лицензия

MIT License

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в `logs/bot.log`
2. Убедитесь в правильности настроек в `.env`
3. Проверьте системные требования
4. Создайте issue в репозитории
